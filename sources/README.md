# Archie Source Library

Each source lives in its own directory and has a `source.yaml` manifest. Source manifests define identity, authority, edition, priority, licensing metadata, and the pinned content version/hash.

v2.0.0-alpha.1 ships with exactly one enabled rules authority: `srd521` (System Reference Document 5.2.1). Open5e is intentionally not integrated in alpha.1.
